<UserControl x:Class="Booking.Views.Scenes.Scheduled"
             Loaded="Window_Loaded"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:Booking.ViewModels"
             xmlns:local="clr-namespace:Booking.Views.Scenes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewModels:ScheduledViewModel/>
    </UserControl.DataContext>
    
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <ItemsControl ItemsSource="{Binding Bookings}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="5">
                        <StackPanel Orientation="Horizontal">
                            <StackPanel>
                                <TextBlock Text="{Binding Realty.Name}" FontSize="14" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="{Binding StartDate}" FontSize="14" Foreground="White"/>
                                <TextBlock Text="{Binding EndDate}" FontSize="14" Foreground="White"/>
                            </StackPanel>
                            <Image Source="{Binding Realty.ImagePath}" Width="100" Height="100" Margin="10,0,0,0"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>


</UserControl>
