<Window x:Class="Booking.Views.Scenes.Sub_Scenes.RealtyView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Booking.Views.Scenes.Sub_Scenes"
        xmlns:viewModels="clr-namespace:Booking.ViewModels"
        mc:Ignorable="d"
        Title="RealtyView" Height="450" Width="800">

    <Window.DataContext>
        <viewModels:RealtyViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibility"/>
    </Window.Resources>

    <Window.Visibility>
        <Binding Path="IsViewVisible" Mode="TwoWay" Converter="{StaticResource BooleanToVisibility}"/>
    </Window.Visibility>
    <Grid>
        
    </Grid>
</Window>
